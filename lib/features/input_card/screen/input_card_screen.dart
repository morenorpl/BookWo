import 'package:flutter/material.dart';

class InputCardScreen extends StatelessWidget {
  const InputCardScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Input Card SCreen')));
  }
}
